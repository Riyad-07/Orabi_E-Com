import React from 'react'

const SignUpTitle = ({title, className}) => {
  return (
    <h2 className={`text-[39px] font-bold font-DM text-primary mb-10 ${className}`}>{title}</h2>
    
  )
}

export default SignUpTitle